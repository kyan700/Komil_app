# 🚀 دليل إعداد وتثبيت كُميل برو

## 📋 متطلبات النظام

### المتطلبات الأساسية
- **متصفح حديث**: Chrome 60+ / Firefox 55+ / Safari 11+ / Edge 79+
- **دعم JavaScript**: مطلوب تفعيل JavaScript
- **IndexedDB**: للتخزين المحلي (متوفر في جميع المتصفحات الحديثة)
- **Service Worker**: للعمل بدون إنترنت (اختياري)

### للتطوير والتخصيص
- **Node.js**: الإصدار 14 أو أحدث
- **خادم ويب محلي**: مثل Live Server أو http-server
- **محرر نصوص**: VS Code أو أي محرر آخر

## 📦 طرق التثبيت

### 1. التثبيت السريع (استخدام مباشر)

1. **تحميل الملفات**
   ```bash
   # تحميل جميع ملفات التطبيق
   wget https://github.com/komeil-pro/komeil-pro/archive/main.zip
   unzip main.zip
   cd komeil-pro-main
   ```

2. **فتح التطبيق**
   ```bash
   # افتح index.html في متصفحك أو استخدم خادم محلي
   npx http-server . -p 3000 -o
   ```

3. **الوصول للتطبيق**
   - افتح المتصفح وانتقل إلى `http://localhost:3000`
   - أو افتح ملف `index.html` مباشرة في المتصفح

### 2. التثبيت على خادم ويب

#### Apache
1. **رفع الملفات**
   ```bash
   # رفع جميع الملفات إلى مجلد الويب
   scp -r komeil-pro/ user@server:/var/www/html/
   ```

2. **تكوين Apache**
   ```bash
   # نسخ تكوين Apache
   sudo cp .htaccess /var/www/html/komeil-pro/
   sudo systemctl reload apache2
   ```

#### Nginx
1. **رفع الملفات**
   ```bash
   # رفع الملفات
   scp -r komeil-pro/ user@server:/var/www/
   ```

2. **تكوين Nginx**
   ```bash
   # نسخ تكوين Nginx
   sudo cp nginx.conf /etc/nginx/sites-available/komeil-pro
   sudo ln -s /etc/nginx/sites-available/komeil-pro /etc/nginx/sites-enabled/
   sudo nginx -t && sudo systemctl reload nginx
   ```

### 3. التثبيت على خدمات الاستضافة

#### GitHub Pages
1. **إنشاء repository جديد**
2. **رفع الملفات إلى branch gh-pages**
3. **تفعيل GitHub Pages من إعدادات المشروع**

#### Netlify
1. **سحب الملفات إلى موقع Netlify**
2. **أو ربط GitHub repository**
3. **التطبيق سيكون جاهز فوراً**

#### Vercel
1. **تثبيت Vercel CLI**
   ```bash
   npm i -g vercel
   ```
2. **نشر التطبيق**
   ```bash
   cd komeil-pro
   vercel
   ```

## 🔧 الإعداد المتقدم

### 1. تخصيص الإعدادات

#### تعديل ملف manifest.json
```json
{
  "name": "اسم تطبيقك المخصص",
  "short_name": "اسم مختصر",
  "theme_color": "#لونك_المفضل",
  "start_url": "/مسار_البداية",
  "scope": "/نطاق_التطبيق"
}
```

#### تعديل الألوان في styles.css
```css
:root {
  --primary-color: #لونك_الأساسي;
  --secondary-color: #لونك_الثانوي;
  --bg-primary: #لون_الخلفية;
}
```

### 2. إعداد قاعدة البيانات

التطبيق يستخدم IndexedDB افتراضياً. لا حاجة لإعداد قاعدة بيانات خارجية.

#### تخصيص إعدادات قاعدة البيانات في database.js
```javascript
class KomeilDB {
    constructor() {
        this.dbName = 'اسم_قاعدة_البيانات';
        this.version = 1;
        // باقي الإعدادات...
    }
}
```

### 3. تفعيل الإشعارات

#### طلب إذن الإشعارات
```javascript
// سيتم طلب الإذن تلقائياً عند تشغيل التطبيق
// يمكنك تخصيص الرسائل في notifications.js
```

#### تكوين Service Worker للإشعارات
```javascript
// تعديل sw.js لإضافة إشعارات مخصصة
self.addEventListener('push', (event) => {
    // تخصيص منطق الإشعارات
});
```

## 🎨 التخصيص والتطوير

### 1. هيكل الملفات
```
komeil-pro/
├── index.html              # الصفحة الرئيسية
├── styles.css              # الأنماط والتصميم
├── app.js                  # المنطق الرئيسي
├── database.js             # إدارة قاعدة البيانات
├── notifications.js        # نظام الإشعارات
├── components.js           # مكونات الواجهة
├── advanced-features.js    # الميزات المتقدمة
├── sw.js                   # Service Worker
├── manifest.json           # تكوين PWA
├── icon.svg                # الأيقونة الرئيسية
├── .htaccess              # تكوين Apache
├── nginx.conf             # تكوين Nginx
├── package.json           # معلومات المشروع
└── README.md              # الوثائق
```

### 2. إضافة ميزات جديدة

#### إنشاء مكون جديد
```javascript
// إضافة في components.js
class NewComponent {
    constructor(options) {
        this.options = options;
        this.init();
    }
    
    init() {
        // منطق التهيئة
    }
}
```

#### إضافة صفحة/عرض جديد
```html
<!-- إضافة في index.html -->
<section id="new-view" class="view">
    <!-- محتوى الصفحة الجديدة -->
</section>
```

```javascript
// إضافة في app.js
case 'new-view':
    await this.loadNewViewData();
    break;
```

### 3. تخصيص الأيقونات

#### إنشاء أيقونات PWA
1. **فتح create-icons.html في المتصفح**
2. **تخصيص التصميم والألوان**
3. **تحميل الأيقونات بأحجام مختلفة**

#### استخدام أيقونات مخصصة
```css
/* تعديل الأيقونات في styles.css */
.icon-custom::before { content: '🎓'; }
.icon-book::before { content: '📚'; }
```

## 🔒 الأمان والأداء

### 1. إعدادات الأمان

#### Content Security Policy
```apache
# في .htaccess
Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline';"
```

#### HTTPS (موصى به للإنتاج)
```nginx
# في nginx.conf
ssl_protocols TLSv1.2 TLSv1.3;
add_header Strict-Transport-Security "max-age=63072000";
```

### 2. تحسين الأداء

#### ضغط الملفات
```apache
# في .htaccess
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/css application/javascript
</IfModule>
```

#### التخزين المؤقت
```nginx
# في nginx.conf
location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

## 📊 المراقبة والصيانة

### 1. مراقبة الأداء
```bash
# استخدام Lighthouse لتحليل الأداء
npm install -g lighthouse
lighthouse http://your-domain.com
```

### 2. النسخ الاحتياطي
```bash
# إنشاء نسخة احتياطية
tar -czf komeil-pro-backup-$(date +%Y%m%d).tar.gz komeil-pro/
```

### 3. التحديثات
```bash
# تحديث Service Worker عند إضافة ملفات جديدة
# تعديل CACHE_NAME في sw.js
const CACHE_NAME = 'komeil-pro-v1.1.0';
```

## 🐛 استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### التطبيق لا يعمل بدون إنترنت
- **السبب**: Service Worker غير مُسجل
- **الحل**: التأكد من تسجيل Service Worker في app.js

#### البيانات لا تُحفظ
- **السبب**: IndexedDB غير مدعوم أو محظور
- **الحل**: التحقق من إعدادات المتصفح

#### الأيقونات لا تظهر
- **السبب**: مشكلة في مسارات الملفات
- **الحل**: التأكد من وجود ملفات الأيقونات في المجلد الصحيح

#### مشكلة في الأداء
- **السبب**: عدم تفعيل الضغط
- **الحل**: التأكد من تكوين خادم الويب بشكل صحيح

## 📞 الدعم والمساعدة

### الحصول على المساعدة
- **الوثائق**: راجع ملف README.md
- **الأمثلة**: انظر إلى التعليقات في الكود
- **المجتمع**: ابحث في Issues على GitHub

### الإبلاغ عن الأخطاء
1. تأكد من تكرار المشكلة
2. اجمع معلومات البيئة (متصفح، نظام تشغيل)
3. قدم خطوات إعادة تكوين المشكلة
4. أرفق لقطات شاشة إن أمكن

## ✅ اختبار التثبيت

### قائمة التحقق
- [ ] التطبيق يفتح بشكل صحيح
- [ ] يمكن إضافة مادة دراسية جديدة
- [ ] يمكن إضافة مهمة جديدة
- [ ] البيانات تُحفظ بعد إعادة تحميل الصفحة
- [ ] التطبيق يعمل بدون إنترنت (إذا كان Service Worker مُفعل)
- [ ] الإشعارات تعمل (إذا كانت مُفعلة)
- [ ] التطبيق مُحسن للهواتف المحمولة
- [ ] يمكن تثبيت التطبيق كـ PWA

---

🎉 **تهانينا! تم إعداد كُميل برو بنجاح**

للمساعدة الإضافية، راجع ملف QUICKSTART.md للبدء السريع أو README.md للدليل الشامل.